<%= link_to 'UnWatch', watches_destroy_path(anime_id: anime_id),
                       method: 'delete', remote: true,
                       class: 'btn btn-danger btn-sm', id: "unwatch_#{anime_id}" %>